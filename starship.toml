"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](#3B4252)\
$os\
$username\
[](bg:#434C5E fg:#3B4252)\
$directory\
[](fg:#434C5E bg:#5E81AC)\
$git_branch\
$git_status\
[](fg:#5E81AC bg:#81A1C1)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
$scala\
[](fg:#81A1C1 bg:#88C0D0)\
$docker_context\
[](fg:#88C0D0 bg:#8FBCBB)\
$time\
[ ](fg:#8FBCBB)\
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#3B4252"
style_root = "bg:#BF616A" # Nord red for root
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#3B4252"
disabled = true # Disabled by default

[directory]
style = "bg:#434C5E"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[cpp]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#88C0D0"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#5E81AC"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#5E81AC"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#81A1C1"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "bg:#8FBCBB"
format = '[  $time ]($style)'

# Additional Nord-themed modules you might want to add
[character]
success_symbol = "[❯](#A3BE8C)"
error_symbol = "[❯](#BF616A)"
vimcmd_symbol = "[❮](#EBCB8B)"

[cmd_duration]
format = "[$duration](#D08770)"
style = "bold #D08770"

[battery]
full_symbol = "🔋"
charging_symbol = "⚡"
discharging_symbol = "💀"
format = "[$symbol$percentage](#A3BE8C)"
style = "bold #A3BE8C"
